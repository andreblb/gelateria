<!DOCTYPE html>
<html>
  <head>
    <title>Gelateria Pro</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <style>
      /* Estilos Profissionais */
      :root { --roxo-acai: #4A148C; --roxo-claro: #7B1FA2; --cinza-fundo: #f4f4f9; }
      body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: var(--cinza-fundo); margin: 0; }
      
      .navbar { background: var(--roxo-acai); padding: 15px 30px; display: flex; justify-content: space-between; align-items: center; box-shadow: 0 2px 5px rgba(0,0,0,0.2); }
      .navbar a { color: white; text-decoration: none; font-weight: bold; margin-left: 20px; }
      
      .main-container { max-width: 900px; margin: 30px auto; background: white; padding: 40px; border-radius: 15px; box-shadow: 0 10px 25px rgba(0,0,0,0.05); }
      
      .form-control { width: 100%; padding: 12px; margin-top: 5px; border: 1px solid #ddd; border-radius: 8px; font-size: 1rem; }
      .btn-primary { background: var(--roxo-acai); color: white; padding: 15px; border: none; border-radius: 8px; width: 100%; cursor: pointer; font-size: 1.1rem; }
      .btn-primary:hover { background: var(--roxo-claro); }
    </style>
  </head>

  <body>
    <nav class="navbar">
      <div style="font-size: 1.5rem;">🍦 <strong>Gelateria Pro</strong></div>
      <div>
        <%= link_to "Início", root_path %>
        <%= link_to "Pedidos", pedidos_path %>
        <%= link_to "Clientes", customers_path %>
        <%= link_to "Sabores", products_path %>
      </div>
    </nav>

    <div class="main-container">
      <%= yield %>
    </div>
  </body>
</html>